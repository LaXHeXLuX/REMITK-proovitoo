import { VehicleDescriptionPipe } from './vehicle-description-pipe';

describe('VehicleDescriptionPipe', () => {
	it('create an instance', () => {
		const pipe = new VehicleDescriptionPipe();
		expect(pipe).toBeTruthy();
	});
});
